// swift-tools-version:5.3

import PackageDescription

let package = Package(
    name: "OpenLogin",
    products: [
        .library(
            name: "OpenLogin",
            targets: ["OpenLogin"]),
    ],
    dependencies: [
    ],
    targets: [
        .target(
            name: "OpenLogin",
            dependencies: []),
    ]
)
